"use strict";

(function () {
  "use strict";

  window.onload = function () {
    var e = document.querySelector(".parallax");

    if (e) {
      var i = function i() {
        c += (u - c) * l, m += (f - m) * l, s.style.cssText = "transform: translate(".concat(c / r, "%, ").concat(m / r, "%);"), a.style.cssText = "transform: translate(".concat(c / o, "%, ").concat(m / o, "%);"), n.style.cssText = "transform: translate(".concat(c / r, "%, ").concat(m / r, "%);"), requestAnimationFrame(i);
      };

      var t = document.querySelector(".parallax__container"),
          s = document.querySelector(".images-parallax__clouds"),
          a = document.querySelector(".images-parallax__mountains"),
          n = document.querySelector(".images-parallax__human"),
          r = 40,
          o = 20,
          l = .05;
      var c = 0,
          m = 0,
          u = 0,
          f = 0;
      i(), e.addEventListener("mousemove", function (t) {
        var s = e.offsetWidth,
            a = e.offsetHeight,
            n = t.pageX - s / 2,
            r = t.pageY - a / 2;
        u = n / s * 100, f = r / a * 100;
      });
      var d = [];

      for (var y = 0; y <= 1; y += 5e-4) {
        d.push(y);
      }

      new IntersectionObserver(function (s, r) {
        var o;
        o = window.pageYOffset / e.offsetHeight * 100, t.style.cssText = "transform: translate(0%, -".concat(o / 9, "%);"), a.parentElement.style.cssText = "transform: translate(0%, -".concat(o / 6, "%);"), n.parentElement.style.cssText = "transform: translate(0%, -".concat(o / 3, "%);");
      }, {
        threshold: d
      }).observe(document.querySelector(".content"));
    }
  };
})();